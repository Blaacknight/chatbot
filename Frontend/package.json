{
  "name": "frontend",  
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",               // Update the version as necessary
    "react-dom": "^17.0.2",           // Update the version as necessary
    "react-scripts": "4.0.3",          // Update the version as necessary
    "serve": "^14.0.1"                 // Added serve package
    // Add any other dependencies your project requires
  },
  "scripts": {
    "start": "serve -s build",         // Start script to serve the build
    "build": "react-scripts build",    // Build script
    "test": "react-scripts test",      // Test script
    "eject": "react-scripts eject"     // Eject script
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
